# Type99 — fusion tank (vehicle_png)
#
# Same prepare/publish/qa flow as PLA; see tools/asset_pipeline/templates/type99/manifest.yaml
# Engine uses PNG body+turret (vxl.cpp stems null) — MagicaVoxel VXL can replace later via kind: vehicle.
